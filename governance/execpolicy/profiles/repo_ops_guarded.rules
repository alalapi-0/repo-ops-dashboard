# repo-ops-dashboard guarded implementation profile.
# This profile only adds restrictions; current user and higher policy grant authority.
deny read **/.env
deny read **/.env.*
deny read **/*secret*
deny read **/*token*
prompt command git push
prompt command rm -rf
